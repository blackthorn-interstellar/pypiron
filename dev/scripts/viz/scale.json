{
  "kind": "scale",
  "title": "How much of this has actually been run",
  "_note": [
    "Every entry MUST carry a non-empty `source` naming the file and line it was",
    "measured at, plus the commit it was measured on where the source records one.",
    "build.py refuses to emit a number without one, and refuses two sources it is",
    "never honest to quote outward: `make dr-drill` (sub-second at N=10 —",
    "dev/TESTING.md:149-153) and `make perf` (the Python client harness is the",
    "bottleneck — dev/TESTING.md:2017).",
    "",
    "Every `source` anchor here is `path:line` and was re-read at the cited line.",
    "Anchors rot on the next refactor of the file they point into: re-derive them,",
    "do not carry them forward.",
    "",
    "`live: true` rows are re-measured on the building machine at build time and are",
    "labelled as such by the player; they are never merged with the committed",
    "six-figure measurements. `headline` picks the odometer.",
    "",
    "The word 'depth' is deliberately absent: the model checkers' 38/43 come from a",
    "multi-threaded BFS and are machine-dependent, so they are not protocol facts."
  ],

  "stats": [
    {
      "headline": true,
      "value": 500651992,
      "label": "storage-op interleavings executed with zero invariant violations — aligned lane",
      "source": "dev/TESTING.md:553 · a 900-second lane measured at commit 8e5916f. Aligned lane only (`--rotate --require-reach`, the one row in that table without --partition 100). The three partitioned lanes beside it also read zero failing, but the narrow one is starved by construction and its seeds must never be pooled with the rest (dev/TESTING.md:556-563)"
    },
    {
      "value": 289934,
      "label": "seeded fault schedules in that one run",
      "source": "dev/TESTING.md:553 · aligned lane, commit 8e5916f"
    },
    {
      "value": 1510651,
      "label": "acked uploads verified byte-for-byte in that run",
      "source": "dev/TESTING.md:553 · aligned lane, commit 8e5916f"
    },
    {
      "value": 1497544,
      "label": "protocol states checked exhaustively on every merge",
      "source": "tests/model_event_protocol.rs:952 CI_STATES · hand-recorded from the `[event_protocol] unique states` line `model_event_protocol` prints (:866); the state count is printed, never asserted"
    },
    {
      "value": 3354376,
      "label": "protocol states checked exhaustively, nightly",
      "source": "tests/model_event_protocol.rs:954 CI_STATES_NIGHTLY · hand-recorded the same way from the nightly `model_event_protocol_deep` job (:876); printed, never asserted"
    },
    {
      "value": "27 / 27",
      "label": "deliberate defects, each red for the right reason",
      "source": ".github/workflows/ci.yml · 27 `check` legs over 26 `--break` names in the “Oracle kill proofs (--break)” step, on every merge"
    },
    {
      "value": 27,
      "label": "oracle and classifier slots · 26 with a proven --break kill. The 27th, MERGE_DIVERGENCE, is a coverage statistic and not a gate: it owns no violation string, so nothing can red it and no mutation can kill it. The 27 legs cover 26 slots because STALENESS has two arms — `wedge` (never agrees) and `slow-repair` (agrees, too late)",
      "source": "examples/vopr.rs REACH_SLOTS (reach panel printed on every run) for the 27 slots; examples/vopr.rs `const BREAKS: [...; 26]` and the 27 `check` legs in .github/workflows/ci.yml for the legs. The slot no break can kill is MERGE_DIVERGENCE, where REACH_VIOLATION_PREFIX is the empty string; the eleven classifier slots are empty there too but are killable through the `AUDIT_*` strings their oracle pushes"
    },
    {
      "value": "1-2 billion",
      "label": "seeded fault schedules per month, at $25 of spot",
      "source": "dev/ops/soak/README.md:59 · budgeted at ~400 seeds/s/vCPU (:48)"
    }
  ],

  "bars": [
    {
      "label": "DURABILITY exempts a `.frozen` filename — 0 times in 20,000 aligned seeds",
      "value": 132586,
      "source": "dev/TESTING.md:520 · 130,331 seeds at --partition 100"
    },
    {
      "label": "CONSERVATION finds an acked body only in a `_quarantine/` copy — 0 aligned",
      "value": 99630,
      "source": "dev/TESTING.md:524 · 130,331 seeds at --partition 100"
    },
    {
      "label": "DURABILITY sees bytes no ack carried — 0 aligned",
      "value": 14039,
      "source": "dev/TESTING.md:525 · 130,331 seeds at --partition 100"
    },
    {
      "label": "…and the `_quarantine/` preservation clause excuses it — 0 aligned",
      "value": 13150,
      "source": "dev/TESTING.md:526 · 130,331 seeds at --partition 100"
    },
    {
      "label": "VISIBILITY exempts what the renderer omits — 0 aligned",
      "value": 571,
      "source": "dev/TESTING.md:522 · 130,331 seeds at --partition 100"
    }
  ]
}
